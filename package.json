{
  "name": "formulario-cursos",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "server": "json-server --watch db.json --port 3000",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "zod": "^3.25.63"
  }
}
